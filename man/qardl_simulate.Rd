% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qardl_simulate.R
\name{qardl_simulate}
\alias{qardl_simulate}
\title{Monte Carlo Simulation for QARDL}
\usage{
qardl_simulate(
  nobs = 200L,
  reps = 1000L,
  tau = c(0.25, 0.5, 0.75),
  p = 1L,
  q = 1L,
  k = 1L,
  beta_true = NULL,
  phi_true = NULL,
  gamma_true = NULL,
  sigma_u = 1,
  sigma_x = 1,
  seed = NULL,
  parallel = FALSE,
  ncores = NULL
)
}
\arguments{
\item{nobs}{Integer. Sample size for each simulation. Default is 200.}

\item{reps}{Integer. Number of Monte Carlo replications. Default is 1000.}

\item{tau}{Numeric vector of quantiles. Default is \code{c(0.25, 0.50, 0.75)}.}

\item{p}{Integer. AR lag order. Default is 1.}

\item{q}{Integer. Distributed lag order. Default is 1.}

\item{k}{Integer. Number of covariates. Default is 1.}

\item{beta_true}{Numeric vector. True long-run parameters (length k).
Default is \code{rep(1, k)}.}

\item{phi_true}{Numeric vector. True AR parameters (length p).
Default is \code{rep(0.5, p)}.}

\item{gamma_true}{Numeric vector. True impact parameters (length k).
Default is \code{rep(0.3, k)}.}

\item{sigma_u}{Numeric. Standard deviation of the error term. Default is 1.}

\item{sigma_x}{Numeric. Standard deviation of covariate innovations. Default is 1.}

\item{seed}{Integer. Random seed for reproducibility. Default is \code{NULL}.}

\item{parallel}{Logical. Use parallel processing. Default is \code{FALSE}.}

\item{ncores}{Integer. Number of cores for parallel processing.
Default is \code{parallel::detectCores() - 1}.}
}
\value{
An object of class \code{"qardl_mc"} containing:
\describe{
  \item{beta_sim}{Array of simulated beta estimates (k x ntau x reps)}
  \item{phi_sim}{Array of simulated phi estimates (p x ntau x reps)}
  \item{gamma_sim}{Array of simulated gamma estimates (k x ntau x reps)}
  \item{beta_true}{True beta values}
  \item{phi_true}{True phi values}
  \item{gamma_true}{True gamma values}
  \item{bias_beta}{Bias in beta estimates}
  \item{rmse_beta}{RMSE of beta estimates}
  \item{coverage_beta}{Empirical coverage of 95\% CI for beta}
  \item{reps}{Number of replications}
  \item{nobs}{Sample size}
  \item{tau}{Vector of quantiles}
}
}
\description{
Performs Monte Carlo simulation to assess the finite-sample properties
of QARDL estimators under specified data generating processes.
}
\details{
The data generating process is:
\deqn{y_t = \sum_{i=1}^{p} \phi_i y_{t-i} + \sum_{j=1}^{k} \gamma_j x_{jt} + u_t}

where \eqn{u_t \sim N(0, \sigma_u^2)} and \eqn{x_{jt}} follows a random walk
with innovations \eqn{\sim N(0, \sigma_x^2)}.
}
\examples{
# Small simulation for illustration
mc <- qardl_simulate(nobs = 100, reps = 50, tau = c(0.25, 0.50, 0.75),
                     p = 1, q = 1, k = 1, seed = 123)
print(mc)

}
\references{
Cho, J.S., Kim, T.-H., & Shin, Y. (2015). Quantile cointegration in the
autoregressive distributed-lag modeling framework. \emph{Journal of
Econometrics}, 188(1), 281-300. \doi{10.1016/j.jeconom.2015.01.003}
}
\seealso{
\code{\link{qardl}}, \code{\link{print.qardl_mc}}
}
